startCommand:
  type: http
  configSchema:
    type: object
    properties: {}
  commandFunction: |
    (config) => ({url: 'https://rumblingb.github.io/mcp-directory'})

displayName: "MCP Directory"
description: "Curated directory of MCP (Model Context Protocol) servers for AI agents — discover and compare MCP tools."
categories:
  - "MCP"
  - "Directory"
  - "Discovery"
